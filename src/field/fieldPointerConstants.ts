/** Matches `SHADER` in `pennant-landing.html` reference. */
export const FIELD_POINTER = {
  mouseLerp: 0.12,
  enterSpeed: 0.18,
  settleSpeed: 0.045,
  velLerpFast: 0.25,
  velLerpSlow: 0.05,
} as const;
